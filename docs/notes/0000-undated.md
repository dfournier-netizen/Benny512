# Benny512 — notes preamble and undated sections

# Benny512 — Project Notes

**Append-only chronological changelog.** Never edit or delete prior entries — add a new dated section at the bottom for every phase or significant event. For current-state (not history), see the latest `Benny512_handoff_<timestamp>.md` in this folder.

---

## Phase 1a (Swift implementation)

Swift implementation of the wire-format layer (`ByteReader`/`ByteWriter`, `ArtNetCodec`, `RDMCodec`) built against the verified fixtures. Result: **113 passing tests**, archived (not deleted) at `Benny512/archive-swift-v1/LightingKit` — independently re-confirmed present with 113 `func test...` occurrences during the 2026-08-09 handoff/notes creation pass.
